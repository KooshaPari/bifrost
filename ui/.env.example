# Public Bifrost HTTP API origin (no trailing slash, no /api suffix).
# Required when the UI is hosted separately from the Bifrost gateway (e.g. Vercel).
# Example: https://bifrost.example.com
VITE_BIFROST_API_BASE_URL=
